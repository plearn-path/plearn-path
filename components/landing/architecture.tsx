const mvp = ["Next.js App Router + API Routes", "PostgreSQL + prerequisite adjacency table", "Gemini structured hint (synchronous demo)", "PWA + IndexedDB offline cache"];
const vision = ["Go scoring microservice + NGINX", "Redis Cluster session state", "RabbitMQ for LLM jobs", "Neo4j for complex knowledge graphs + AIS Edge"];
export function Architecture() { return <section id="architecture" className="bg-ink py-20 text-white"><div className="mx-auto max-w-7xl px-5 lg:px-8"><p className="text-sm font-bold text-sun">ARCHITECTURE WITH HONEST SCOPE</p><h2 className="mt-3 text-3xl font-bold">เดโมที่ทำได้จริงวันนี้<br />พร้อมทางไปสู่ระบบที่ scale ได้</h2><div className="mt-10 grid gap-5 lg:grid-cols-2"><ArchitectureCard title="MVP DEMO · BUILD NOW" description="สิ่งที่ใช้ในการสาธิตวันแข่ง" items={mvp} accent="bg-mint" /><ArchitectureCard title="VISION · SCALE LATER" description="แผนต่อยอดเมื่อมีผู้ใช้และข้อมูลมากขึ้น" items={vision} accent="bg-sun" /></div><p className="mt-7 text-xs leading-6 text-slate-400">MVP ใช้ API Routes และ mock data เป็นหลัก · TODO: extract scoring to Go microservice + RabbitMQ when scaling.</p></div></section>; }
function ArchitectureCard({ title, description, items, accent }: { title: string; description: string; items: string[]; accent: string }) { return <article className="rounded-2xl border border-white/15 bg-white/5 p-6"><div className="flex items-center gap-3"><span className={`h-3 w-3 rounded-full ${accent}`} /><div><p className="text-xs font-bold tracking-wider text-slate-200">{title}</p><p className="mt-1 text-sm text-slate-400">{description}</p></div></div><ul className="mt-6 space-y-2">{items.map((item) => <li key={item} className="rounded-lg bg-white/5 px-3 py-2 text-sm">{item}</li>)}</ul></article>; }
