const fastPath = ["Next.js Student App", "Fast Scoring Engine", "Redis Session", "Next Question < 50ms"];
const slowPath = ["RabbitMQ Queue", "Gemini Structured Output", "PostgreSQL Sync", "Teacher Dashboard"];
export function Architecture() { return <section id="architecture" className="bg-ink py-20 text-white"><div className="mx-auto max-w-7xl px-5 lg:px-8"><p className="text-sm font-bold text-sun">SYSTEM ARCHITECTURE</p><h2 className="mt-3 text-3xl font-bold">เร็วสำหรับการเรียนรู้<br />ลึกสำหรับการวิเคราะห์</h2><p className="mt-4 max-w-xl text-sm leading-6 text-slate-300">Decoupled architecture แยกงานตอบสนองทันที ออกจากงาน AI ที่ใช้เวลาประมวลผลมากกว่า</p><div className="mt-10 grid gap-5 lg:grid-cols-2"><PathCard title="FAST PATH" subtitle="ตอบกลับผู้เรียนแบบ real-time" items={fastPath} accent="bg-mint" /><PathCard title="SLOW PATH" subtitle="วิเคราะห์และสร้างคำใบ้เบื้องหลัง" items={slowPath} accent="bg-sun" /></div><div className="mt-8 flex flex-wrap gap-2">{["Next.js", "Go / FastAPI", "Redis", "Neo4j", "RabbitMQ", "Gemini API", "PostgreSQL"].map((tech) => <span key={tech} className="rounded-full border border-white/20 px-3 py-1.5 text-xs font-semibold text-slate-200">{tech}</span>)}</div></div></section>; }
function PathCard({ title, subtitle, items, accent }: { title: string; subtitle: string; items: string[]; accent: string }) { return <article className="rounded-2xl border border-white/15 bg-white/5 p-6"><div className="flex items-center gap-3"><span className={`h-3 w-3 rounded-full ${accent}`} /><div><p className="text-xs font-bold tracking-wider text-slate-300">{title}</p><p className="mt-1 text-sm text-slate-400">{subtitle}</p></div></div><div className="mt-6 space-y-2">{items.map((item, index) => <div key={item} className="flex items-center gap-3 rounded-lg bg-white/5 px-3 py-2 text-sm"><span className="text-slate-500">{index + 1}</span>{item}</div>)}</div></article>; }
